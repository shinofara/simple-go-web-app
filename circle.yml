machine:
  environment:
    PATH: "/home/ubuntu/go/bin:$PATH"
    IMPORT_PATH: "github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"
    GOVERSION: 1.7.4
    GOROOT: "/home/ubuntu/go"
    WORK_DIR: "/home/ubuntu/.go_workspace"

dependencies:
  cache_directories:
    - "/home/ubuntu/.go"
  pre:
    - cd /home/ubuntu;
      if [[ ! -e /home/ubuntu/.go/$GOVERSION/bin/go ]]; then
        curl https://storage.googleapis.com/golang/go$GOVERSION.linux-amd64.tar.gz | tar -xz;
        go get -u github.com/jstemmer/go-junit-report;
        go get -u github.com/golang/dep/...;
        go get -u github.com/Masterminds/glide;
        mkdir -p ~/.go;
        cp -rp ~/go ~/.go/$GOVERSION;
      else
        cp -rp ~/.go/$GOVERSION ~/go;
      fi
    - go version

  override:
    - mkdir -p "$WORK_DIR/src/github.com/$CIRCLE_PROJECT_USERNAME"
    - ln -sfn $HOME/$CIRCLE_PROJECT_REPONAME $WORK_DIR/src/$IMPORT_PATH
    - cd "$WORK_DIR/src/$IMPORT_PATH/" && glide update

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/golang
    - make ci-test
    - make ci-vet
